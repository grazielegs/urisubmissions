# include <stdio.h>

int main()
{
    int distancia, carrox, carroy, resultado;

    scanf("%d", &distancia);

    resultado = distancia * 2;

    printf("%d minutos\n", resultado);

    return 0;
}
